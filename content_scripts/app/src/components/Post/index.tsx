// Components
import { Box, Card } from '@mantine/core';

type ListProps = {
  //   list: ListDataInfo[];
};

function List(props: ListProps) {
  //   const { list } = props;
  return <Box></Box>;
}

export default List;
