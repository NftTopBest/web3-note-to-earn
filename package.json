{
  "name": "react-browser-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "./build.sh",
    "zip": "(rm packed.zip || true) && zip -r packed.zip build/"
  },
  "devDependencies": {
    "stream-browserify": "^3.0.0",
    "web-ext": "^6.6.0"
  },
  "dependencies": {
    "web3": "^1.7.3"
  }
}
